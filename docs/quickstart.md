# Quickstart

1. Create a repository.
2. Create a schedule definition with bounds and default slot size.
3. Instantiate `Scheduler`.
4. Add rules if you need custom constraints.
5. Book events and generate slots.

