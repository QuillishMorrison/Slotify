# Concepts

- `TimeRange`: immutable interval normalized to UTC.
- `Event`: a source-of-truth occupancy record.
- `ScheduleDefinition`: schedule bounds and defaults.
- `Slot`: derived free or occupied window.
- `Rule`: pluggable validation unit.
- `SlotGenerationStrategy`: free slot derivation algorithm.

