# Extensibility

- Add custom rules by implementing the `Rule` protocol.
- Add custom repositories by implementing `ScheduleRepository`.
- Add custom slot generation by implementing `SlotGenerationStrategy`.
- Add adapters in separate packages to keep the core lean.

