# Overview

`slotify` is a headless scheduling engine built around three ideas:

1. events are the source of truth
2. slots are derived artifacts
3. policies belong in explicit rules and strategies

The core is intentionally neutral and does not model domain-specific concepts such as staff, orders, or CRM workflows.

